@model IEnumerable<Lorenzo_InterTransit_MVC.MAD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MAD_LIEU_ENLEV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAD_ADRES_ENLEV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAD_DATE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAD_OBS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MAD_LIEU_ENLEV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAD_ADRES_ENLEV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAD_DATE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAD_OBS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MAD_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MAD_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MAD_ID })
        </td>
    </tr>
}

</table>
